# Приведение логической модели к третьей нормальной форме

## 1. Цель нормализации

На этапе нормализации логическая модель PostgreSQL была проверена на соответствие первой, второй и третьей нормальным формам. Целью нормализации является устранение избыточности данных, предотвращение аномалий вставки, изменения и удаления, а также обеспечение согласованности данных.

В качестве основы использована логическая модель PostgreSQL системы сервисного центра, построенная на основе ранее разработанной концептуальной ER-модели.

В модели используются суррогатные идентификаторы `id` в качестве первичных ключей таблиц. Связи между сущностями реализуются с помощью внешних ключей.

## 2. Проверка первой нормальной формы

Модель соответствует требованиям **первой нормальной формы (1НФ)**.

Каждая таблица содержит атомарные атрибуты: одно поле хранит одно значение, отсутствуют повторяющиеся группы атрибутов и массивы значений.

Например, таблица `client` содержит отдельные атрибуты:

- `id`;
- `full_name`;
- `phone`;
- `email`.

Таблица `equipment` также содержит отдельные атомарные характеристики оборудования:

- `id`;
- `client_id`;
- `type`;
- `manufacturer`;
- `model`;
- `serial_number`;
- `condition_description`;
- `status`.

Аналогичный принцип применяется к остальным таблицам модели.

**Вывод:** все атрибуты представлены в атомарном виде, поэтому модель соответствует 1НФ.

## 3. Проверка второй нормальной формы

**Вторая нормальная форма (2НФ)** требует, чтобы таблица уже находилась в 1НФ и каждый неключевой атрибут полностью зависел от первичного ключа.

В итоговой модели в качестве первичных ключей используются отдельные атрибуты `id`. Поэтому частичные зависимости неключевых атрибутов от части составного ключа отсутствуют.

Отдельно была рассмотрена таблица `employee`. В исходном варианте атрибут `specialization` ошибочно был включён в состав первичного ключа вместе с `id`. Поскольку сотрудник однозначно определяется своим идентификатором, `specialization` не должна являться частью первичного ключа.

После исправления таблица имеет структуру:

```
employee
-------------------------
id             PK
full_name
specialization
phone
email
```

Теперь все неключевые атрибуты сотрудника зависят от `employee.id`:

```
employee.id → full_name
employee.id → specialization
employee.id → phone
employee.id → email
```

Таким образом, частичных зависимостей нет, и таблица соответствует 2НФ.

**Вывод:** после исключения `specialization` из первичного ключа таблицы модели соответствуют требованиям 2НФ.

# 4. Проверка третьей нормальной формы

**Третья нормальная форма (3НФ)** требует отсутствия транзитивных зависимостей неключевых атрибутов от первичного ключа.

При проверке модели была обнаружена транзитивная зависимость в таблице `request`.

В таблице `equipment` хранится информация о владельце оборудования:

```
equipment
-------------------------
id
client_id
...
```

Таким образом:

```
equipment.id → client_id
```

При этом заявка связана с конкретным оборудованием:

```
request.id → equipment_id
```

Следовательно, если одновременно хранить `client_id` в `request`, возникает зависимость:

```
request.id → equipment_id → client_id
```

То есть `client_id` в таблице `request` определяется не непосредственно заявкой, а через связанное с ней оборудование. Это является транзитивной зависимостью и нарушает требования 3НФ.

## 5. Устранение транзитивной зависимости

Для устранения избыточности атрибут `client_id` был удалён из таблицы `request`.

Итоговая структура заявки содержит связь с оборудованием:

```
request
-------------------------
id             PK
equipment_id   FK
operator_id    FK
created_at
received_at
description
status
waiting_reason
preliminary_cost
total_cost
client_approved
approval_at
issued_at
completed_at
```

Информация о клиенте теперь получается через связанное оборудование:

```
request
   ↓
equipment
   ↓
client
```

То есть:

```
request.equipment_id
        ↓
equipment.client_id
        ↓
client.id
```

Таким образом, информация о владельце оборудования хранится только в одном месте — в таблице `equipment`.

Это позволяет избежать ситуации, когда в `request.client_id` и `equipment.client_id` находятся разные значения для одного оборудования.

**Вывод:** удаление `client_id` из `request` устраняет транзитивную зависимость и приводит таблицу к 3НФ.

# 6. Дополнительные изменения для соответствия связям модели

В процессе проверки также были скорректированы ограничения, чтобы физическая структура PostgreSQL соответствовала кардинальностям концептуальной модели.

### Таблица `work`

В исходной версии для `work.request_id` было установлено ограничение `UNIQUE`.

Это означало бы, что для одной заявки можно создать только одну запись `work`.

Однако концептуальная модель предусматривает связь:

```
Заявка 1 : N Работа
```

и в предметной области в рамках одной заявки может выполняться несколько работ. Поэтому ограничение `UNIQUE`с `work.request_id` было удалено.

Теперь несколько работ могут ссылаться на одну заявку:

```
request 1 ───── N work
```

Это изменение не связано непосредственно с 3НФ, но необходимо для соответствия логической модели установленным кардинальностям.

### Таблица `payment`

Для `payment.request_id` было добавлено ограничение `UNIQUE`.

Концептуальная модель предусматривает:

```
Заявка 1 : 1 Оплата
```

Поэтому одна заявка должна иметь не более одной записи оплаты.

Ограничение:

```
payment.request_id UNIQUE
```

обеспечивает это требование на уровне базы данных.

# 7. Итоговая проверка 3НФ

После внесения изменений:

|Таблица / изменение|Результат|
|---|---|
|`client`|соответствует 3НФ|
|`equipment`|соответствует 3НФ|
|`request`|`client_id` удалён для устранения транзитивной зависимости|
|`employee`|`specialization` исключена из PK|
|`work`|`UNIQUE` у `request_id` удалён для реализации связи 1:N|
|`payment`|`UNIQUE` у `request_id` добавлен для реализации связи 1:1|
|`diagnosis`|соответствует 3НФ|
|`repair`|соответствует 3НФ|
|`part`|соответствует 3НФ|
|`part_reservation`|соответствует 3НФ|
|`used_part`|соответствует 3НФ|
|`status_history`|соответствует 3НФ|
|`notification`|соответствует 3НФ|

**Итоговый вывод:**

В результате проверки и нормализации логической модели были устранены частичная зависимость в таблице `employee` и транзитивная зависимость в таблице `request`. Дополнительно ограничения уникальности были скорректированы в соответствии с кардинальностями связей предметной области. После внесённых изменений логическая модель соответствует требованиям третьей нормальной формы: каждый неключевой атрибут зависит от ключа, от всего ключа и ни от чего, кроме ключа.

# 8. Решение по намеренной денормализации

В рамках разработки логической модели было принято решение **не применять намеренную денормализацию**.

Все данные хранятся в таблицах, соответствующих их предметной области, а повторное хранение одних и тех же данных не требуется. В частности, информация о клиенте не дублируется в таблице `request`: клиент определяется через оборудование, связанное с заявкой.

Хранение `client_id` одновременно в таблицах `equipment` и `request` привело бы к дублированию данных и могло бы вызвать нарушение согласованности. Например, при изменении владельца оборудования потребовалось бы дополнительно изменять связанные заявки.

 Поэтому команда решила сохранить нормализованную структуру в 3НФ и не вводить намеренную денормализацию. Возможные проблемы производительности предполагается решать средствами PostgreSQL, в частности с помощью индексов и оптимизации запросов, а не дублированием данных.

### Вывод по денормализации

**Намеренная денормализация в текущей версии модели отсутствует.** Модель сохраняется в нормализованном виде, поскольку имеющиеся бизнес-сценарии не требуют дублирования данных для повышения производительности.

# Словарь таблиц и атрибутов

## Краткий словарь таблиц

|Таблица|Назначение|
|---|---|
|`client`|Клиенты сервисного центра|
|`equipment`|Оборудование клиентов|
|`request`|Заявки на обслуживание|
|`employee`|Сотрудники сервисного центра|
|`master_assignment`|Назначения сотрудников на заявки|
|`diagnosis`|Результаты диагностики|
|`work`|Работы в рамках заявок|
|`repair`|Конкретные ремонтные действия|
|`part`|Запчасти и складские остатки|
|`part_reservation`|Резервирование запчастей|
|`used_part`|Фактически использованные запчасти|
|`status_history`|История изменения статусов заявок|
|`notification`|Уведомления по заявкам|
|`payment`|Оплата заявок|

## 1. Таблица `client` — Клиент

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор клиента|
|`full_name`|`VARCHAR(255)`|NOT NULL|ФИО клиента|
|`phone`|`VARCHAR(30)`|—|Контактный номер телефона|
|`email`|`VARCHAR(255)`|—|Адрес электронной почты клиента|

**Назначение таблицы:** хранение основных данных клиентов сервисного центра.

## 2. Таблица `equipment` — Оборудование

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор оборудования|
|`client_id`|`BIGINT`|FK, NOT NULL|Идентификатор владельца оборудования, ссылка на `client.id`|
|`type`|`VARCHAR(100)`|NOT NULL|Тип оборудования|
|`manufacturer`|`VARCHAR(100)`|NOT NULL|Производитель оборудования|
|`model`|`VARCHAR(100)`|NOT NULL|Модель оборудования|
|`serial_number`|`VARCHAR(100)`|—|Серийный номер оборудования|
|`condition_description`|`TEXT`|—|Описание текущего состояния оборудования|
|`status`|`VARCHAR(30)`|NOT NULL|Текущий статус оборудования|

**Назначение таблицы:** хранение сведений о конкретных устройствах клиентов, переданных в сервисный центр.

## 3. Таблица `request` — Заявка

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор заявки|
|`equipment_id`|`BIGINT`|FK, NOT NULL|Оборудование, по которому создана заявка, ссылка на `equipment.id`|
|`operator_id`|`BIGINT`|FK|Сотрудник, создавший или обрабатывающий заявку, ссылка на `employee.id`|
|`created_at`|`TIMESTAMP`|—|Дата и время создания заявки|
|`received_at`|`TIMESTAMP`|—|Дата и время приёма оборудования|
|`description`|`TEXT`|NOT NULL|Описание проблемы или обращения клиента|
|`status`|`VARCHAR(30)`|NOT NULL|Текущий статус заявки|
|`waiting_reason`|`VARCHAR(40)`|—|Причина ожидания приостановленной заявки|
|`preliminary_cost`|`NUMERIC(12,2)`|—|Предварительная стоимость обслуживания|
|`total_cost`|`NUMERIC(12,2)`|—|Итоговая стоимость заявки|
|`client_approved`|`BOOLEAN`|NOT NULL|Факт согласования работ клиентом|
|`approval_at`|`TIMESTAMP`|—|Дата и время согласования|
|`issued_at`|`TIMESTAMP`|—|Дата и время выдачи оборудования клиенту|
|`completed_at`|`TIMESTAMP`|—|Дата и время завершения заявки|

**Назначение таблицы:** хранение обращений клиентов и их текущего состояния в процессе обслуживания.

## 4. Таблица `employee` — Сотрудник

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор сотрудника|
|`full_name`|`VARCHAR(255)`|NOT NULL|ФИО сотрудника|
|`specialization`|`VARCHAR(30)`|—|Специализация сотрудника|
|`phone`|`VARCHAR(30)`|—|Контактный номер телефона|
|`email`|`VARCHAR(255)`|—|Адрес электронной почты|

**Назначение таблицы:** хранение сведений о сотрудниках сервисного центра.

## 5. Таблица `master_assignment` — Назначение мастера

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор назначения|
|`request_id`|`BIGINT`|FK, NOT NULL|Заявка, на которую назначен мастер, ссылка на `request.id`|
|`employee_id`|`BIGINT`|FK, NOT NULL|Назначенный сотрудник, ссылка на `employee.id`|
|`assigned_at`|`TIMESTAMP`|NOT NULL|Дата и время назначения|
|`ended_at`|`TIMESTAMP`|—|Дата и время окончания назначения|
|`is_active`|`BOOLEAN`|NOT NULL|Признак действующего назначения|

**Назначение таблицы:** фиксация назначения сотрудника на конкретную заявку и периода действия назначения.

## 6. Таблица `diagnosis` — Диагностика

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор диагностики|
|`request_id`|`BIGINT`|FK, NOT NULL|Заявка, к которой относится диагностика, ссылка на `request.id`|
|`master_id`|`BIGINT`|FK, NOT NULL|Сотрудник, выполнивший диагностику, ссылка на `employee.id`|
|`performed_at`|`TIMESTAMP`|NOT NULL|Дата и время проведения диагностики|
|`result`|`TEXT`|NOT NULL|Результат диагностики|
|`detected_malfunction`|`TEXT`|—|Выявленная неисправность|

**Назначение таблицы:** хранение результатов проверки оборудования в рамках заявки.

## 7. Таблица `work` — Работа

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор работы|
|`request_id`|`BIGINT`|FK, NOT NULL|Заявка, в рамках которой выполняется работа, ссылка на `request.id`|
|`started_at`|`TIMESTAMP`|—|Дата и время начала работы|
|`ended_at`|`TIMESTAMP`|—|Дата и время окончания работы|
|`status`|`VARCHAR(30)`|NOT NULL|Статус работы|
|`cost`|`NUMERIC(12,2)`|NOT NULL|Стоимость работы|

**Назначение таблицы:** хранение отдельных работ, выполняемых в рамках заявки.

## 8. Таблица `repair` — Ремонт

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор ремонта|
|`work_id`|`BIGINT`|FK, NOT NULL|Работа, в рамках которой выполняется ремонт, ссылка на `work.id`|
|`master_id`|`BIGINT`|FK, NOT NULL|Сотрудник, выполняющий ремонт, ссылка на `employee.id`|
|`description`|`TEXT`|NOT NULL|Описание ремонтного действия|
|`cost`|`NUMERIC(12,2)`|NOT NULL|Стоимость ремонта|
|`result`|`TEXT`|—|Результат выполнения ремонта|
|`started_at`|`TIMESTAMP`|—|Дата и время начала ремонта|
|`ended_at`|`TIMESTAMP`|—|Дата и время окончания ремонта|
|`status`|`VARCHAR(30)`|NOT NULL|Статус ремонта|

**Назначение таблицы:** хранение конкретных ремонтных действий, выполняемых в рамках работы.

## 9. Таблица `part` — Запчасть

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор запчасти|
|`name`|`VARCHAR(255)`|NOT NULL|Наименование запчасти|
|`article`|`VARCHAR(100)`|UNIQUE|Артикул запчасти|
|`price`|`NUMERIC(12,2)`|NOT NULL|Цена одной единицы запчасти|
|`stock_quantity`|`INTEGER`|NOT NULL|Количество запчастей на складе|

**Назначение таблицы:** хранение сведений о запчастях и их остатках на складе.

## 10. Таблица `part_reservation` — Резерв запчасти

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор резерва|
|`repair_id`|`BIGINT`|FK, NOT NULL|Ремонт, для которого резервируется запчасть, ссылка на `repair.id`|
|`part_id`|`BIGINT`|FK, NOT NULL|Резервируемая запчасть, ссылка на `part.id`|
|`quantity`|`INTEGER`|NOT NULL|Количество зарезервированных единиц|
|`reserved_at`|`TIMESTAMP`|NOT NULL|Дата и время создания резерва|
|`status`|`VARCHAR(30)`|NOT NULL|Статус резерва|

**Назначение таблицы:** хранение информации о запчастях, зарезервированных для выполнения конкретного ремонта.

## 11. Таблица `used_part` — Использованная запчасть

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор записи|
|`repair_id`|`BIGINT`|FK, NOT NULL|Ремонт, в котором использована запчасть, ссылка на `repair.id`|
|`part_id`|`BIGINT`|FK, NOT NULL|Использованная запчасть, ссылка на `part.id`|
|`quantity`|`INTEGER`|NOT NULL|Количество использованных единиц|
|`written_off_at`|`TIMESTAMP`|NOT NULL|Дата и время списания запчасти|

**Назначение таблицы:** фиксация фактически использованных при ремонте запчастей и их списания со склада.

## 12. Таблица `status_history` — История статуса

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор записи истории|
|`request_id`|`BIGINT`|FK, NOT NULL|Заявка, состояние которой изменилось, ссылка на `request.id`|
|`old_status`|`VARCHAR(30)`|—|Предыдущий статус заявки|
|`new_status`|`VARCHAR(30)`|NOT NULL|Новый статус заявки|
|`reason`|`VARCHAR(255)`|—|Причина изменения статуса|
|`changed_at`|`TIMESTAMP`|NOT NULL|Дата и время изменения статуса|

**Назначение таблицы:** хранение истории изменения статусов заявок.

## 13. Таблица `notification` — Уведомление

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор уведомления|
|`request_id`|`BIGINT`|FK, NOT NULL|Заявка, с которой связано уведомление, ссылка на `request.id`|
|`event_type`|`VARCHAR(50)`|NOT NULL|Тип события, вызвавшего уведомление|
|`created_at`|`TIMESTAMP`|NOT NULL|Дата и время создания уведомления|
|`delivery_status`|`VARCHAR(30)`|NOT NULL|Статус доставки уведомления|

**Назначение таблицы:** хранение уведомлений, сформированных системой по событиям, связанным с заявками.


## 14. Таблица `payment` — Оплата

|Атрибут|Тип данных|Ограничения|Описание|
|---|---|---|---|
|`id`|`BIGSERIAL`|PK|Уникальный идентификатор оплаты|
|`request_id`|`BIGINT`|FK, UNIQUE, NOT NULL|Заявка, к которой относится оплата, ссылка на `request.id`|
|`amount`|`NUMERIC(12,2)`|NOT NULL|Сумма оплаты|
|`paid_at`|`TIMESTAMP`|—|Дата и время оплаты|
|`method`|`VARCHAR(30)`|—|Способ оплаты|
|`status`|`VARCHAR(30)`|NOT NULL|Статус оплаты|

**Назначение таблицы:** хранение информации об оплате по заявке.



## 15. Матрица «Таблица → Владелец данных → Бизнес-сценарий»

Матрица показывает, какая таблица базы данных отвечает за хранение определённого типа информации, кто является владельцем этих данных с точки зрения бизнес-логики и в рамках какого бизнес-сценария эти данные создаются, изменяются или используются.

| Таблица | Владелец данных | Основной бизнес-сценарий | Дополнительные сценарии |
| :--- | :--- | :--- | :--- |
| `client` | Администратор / Оператор | **1. Создание заявки на ремонт** — регистрация нового клиента или выбор существующего. | 6. Выдача оборудования и закрытие заявки (идентификация клиента). |
| `equipment` | Администратор / Оператор | **1. Создание заявки на ремонт** — регистрация оборудования клиента или выбор уже существующего. | 2. Приём оборудования и назначение мастера (проверка соответствия данных). |
| `employee` | Руководитель / Системный администратор | **2. Приём оборудования и назначение мастера** — выбор мастера для заявки. | 3. Проведение диагностики (мастер выполняет диагностику). |
| `request` | Администратор / Оператор | **1. Создание заявки на ремонт** — создание новой заявки со статусом «Создана». | 2. Приём оборудования (статус «Принята»).<br>3. Проведение диагностики (статус «На диагностике», «Ожидание»).<br>4. Согласование ремонта (статус «В ремонте»).<br>5. Выполнение и завершение ремонта (статус «Готова»).<br>6. Выдача оборудования и закрытие заявки (статус «Закрыта»). |
| `master_assignment` | Администратор / Оператор | **2. Приём оборудования и назначение мастера** — создание записи о назначении ответственного мастера. | 5. Выполнение и завершение ремонта (замена мастера при необходимости). |
| `diagnosis` | Мастер / Инженер | **3. Проведение диагностики** — фиксация результатов проверки оборудования и выявленных неисправностей. | 4. Согласование ремонта (результаты диагностики используются для формирования перечня работ). |
| `work` | Мастер / Инженер | **3. Проведение диагностики** — формирование комплекса работ на основе результатов диагностики. | 4. Согласование ремонта (клиент согласовывает стоимость работ).<br>5. Выполнение и завершение ремонта (изменение статуса работы). |
| `repair` | Мастер / Инженер | **5. Выполнение и завершение ремонта** — фиксация конкретных ремонтных действий в рамках работы. | 3. Проведение диагностики (предварительное планирование ремонтов).<br>4. Согласование ремонта (корректировка состава работ). |
| `part` | Мастер / Инженер (или Кладовщик) | **5. Выполнение и завершение ремонта** — использование запчастей при выполнении ремонта. | 3. Проведение диагностики (проверка наличия запчастей). |
| `part_reservation` | Мастер / Инженер | **5. Выполнение и завершение ремонта** — резервирование запчастей для конкретного ремонта. | 3. Проведение диагностики (резервирование при планировании работ). |
| `used_part` | Мастер / Инженер | **5. Выполнение и завершение ремонта** — списание запчастей после завершения конкретного ремонта. | — |
| `payment` | Администратор / Оператор | **6. Выдача оборудования и закрытие заявки** — фиксация факта полной оплаты по заявке. | 4. Согласование ремонта (предварительный расчёт стоимости). |
| `status_history` | Система (автоматически) | **1–6. Все бизнес-сценарии** — автоматическая фиксация каждого перехода заявки из одного статуса в другой. | — |
| `notification` | Система (автоматически) | **3. Проведение диагностики** — уведомление клиента о результатах и стоимости. | 1. Создание заявки (подтверждение регистрации).<br>5. Завершение ремонта (уведомление о готовности).<br>6. Закрытие заявки (уведомление о выдаче). |

### Пояснения к матрице

- **Владелец данных** — это роль или компонент системы, который инициирует создание или изменение данных в таблице. Например, таблица `client` заполняется Администратором/Оператором при регистрации клиента, а таблица `status_history` заполняется автоматически системой при смене статуса заявки.
- **Основной бизнес-сценарий** — сценарий, в рамках которого таблица играет ключевую роль (например, `diagnosis` — это центральная таблица для сценария «Проведение диагностики»).
- **Дополнительные сценарии** — другие сценарии, в которых таблица также участвует, но не является главной.

### Связь с бизнес-правилами

Матрица согласована с бизнес-правилами и критериями приёмки:

- Правило 4: приём устройства переводит заявку в статус «Принята» и запускает подбор мастера (таблицы `request`, `master_assignment`).
- Правило 6: переход в статус «Ожидание» возможен только при заполненных результатах диагностики (таблицы `diagnosis`, `request`).
- Правило 7: ремонт начинается только после подтверждения клиента (таблица `request`, поле `client_approved`).
- Правило 10: запчасть списывается только при завершении конкретного ремонта (таблицы `repair`, `used_part`).
- Правило 13: заявка закрывается через единую оплату (таблицы `payment`, `request`).
- Правило 15: каждая смена статуса заявки фиксируется в истории статусов (таблица `status_history`).
